import { CheckOutlined } from '@ant-design/icons'
import { IconBaseProps } from '@ant-design/icons/lib/components/Icon'

export const CheckmarkIcon = ({ title }: IconBaseProps) => (
  <CheckOutlined title={title} />
)
