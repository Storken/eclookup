import { breakpoints } from './breakpoints'
import { borderRadius, borders } from './borders'
import { colors } from './colors'
import { spacings } from './spacings'
import { shadows } from './shadows'

export const theme = {
  spacings,
  colors,
  borders,
  borderRadius,
  breakpoints,
  shadows
}
