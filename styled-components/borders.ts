import { colors } from './colors'

export const borders = {
  light: `solid 2px ${colors.gray.light}`,
  dark: `solid 1px ${colors.gray.dark}`
}

export const borderRadius = {
  md: '3px',
  lg: '24px'
}
